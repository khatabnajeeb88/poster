"use client"

import * as React from "react"
import * as SeparatorPrimitive from "@radix-ui/react-separator"

import { cn } from "@/lib/utils"

export const Separator = ({ orientation, className }) => {
  return <div className={className} />
}
